def pair_difference(arr, k):
    for x in arr:
        for y in arr:
            if x != y and abs(x - y) == k:
                return x, y
    return None
arr = [1, 5, 3, 8]
k = 2
print(pair_difference(arr, k))
