def equilibrium_index(arr):
    for i in range(len(arr)):
        left = sum(arr[:i])
        right = sum(arr[i + 1:])
        if left == right:
            return i
    return -1
arr = [1, 3, 5, 2, 2]
print(equilibrium_index(arr))
