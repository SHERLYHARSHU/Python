def first_repeating_element(arr):
    seen = set()
    for num in arr:
        if num in seen:
            return num
        seen.add(num)
    return None
arr = [1, 2, 3, 2, 4]
print(first_repeating_element(arr))
