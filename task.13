def triplets_sum_zero(arr):
    result = []
    for x in arr:
        for y in arr:
            for z in arr:
                if x < y < z and x + y + z == 0:
                    result.append((x, y, z))
    return result
arr = [-1, 0, 1, 2, -2]
print(triplets_sum_zero(arr))
